# Session: 2026-09-21 11:31 (start)

> **Live-logged**, from the system clock (`date`), same as the `10:30` session.
>
> One task: **BUILD ORDER step 13** — finalise `/docs` (re-walking all five `GETTING_STARTED.md`
> checkpoints against the finished app), `/README.md`, and `.claude/skills/`. This is the last
> numbered step.

- **Starting commit**: `28084c4` — "Updated Steps 11 and 12". Working tree **clean** at session
  start, which the previous session ended without being.
- **Spec version**: `a912316`, content hash `80416c78de8e251f77cc9353727138fd21676427` — unchanged
  since first tracked.

## Log

- `11:31` — **human**: _"Let's execute step 13 now, repo is clean."_
- `11:31` — **the session's first real decision, and it changes what step 13 can claim.** Step 13's
  explicit requirement is to re-walk all five checkpoints *against the finished app*, and two of them
  (3 and 4) are browser checkpoints. Every previous walk covered those over the API and recorded the
  gap honestly rather than closing it — which also left five Definition-of-Done clauses open, all of
  the form "a human needs to look at it". Found **Playwright 1.63.0 available via `npx`** with no
  browser downloaded. Installing Chromium, because a real browser is the difference between closing
  step 13 and re-recording the same gap a third time.

- `11:33`–`11:45` — **checkpoints 1 and 2 walked, and the browser half of 3.** CP1: JDK 26.0.2.1,
  Maven 3.9.16, Node v22.23.2, Docker server 29.5.3. CP2 against a **fresh** database:
  `Started SaasInvestigatorApplication in 2.011 seconds`, `MongoDB indexes verified.`,
  `DEFAULT ADMIN CREDENTIAL CREATED`, and all six documented checks matching the doc exactly
  (`totalElements: 3`, `LLM_CREDENTIAL_ADDED {"provider": "ANTHROPIC"}`,
  `{"configured":true,"source":"AUTO_GENERATED"}`, and no key or tail leaked anywhere in the
  response). Also closed en route: a token minted **before** a backend restart still returned 200
  afterwards, with `jwt-secret` source still `AUTO_GENERATED` and zero re-seed banners — the
  persisted-secret clause, previously argued from code.
- `11:45`–`11:52` — **[`tools/verify-checkpoint3.mjs`](../tools/verify-checkpoint3.mjs): 11 of 11.**
  Two of the five open Definition-of-Done clauses are now closed by measurement rather than by
  reading CSS: dark mode is **navy, not black** (`rgb(255,255,255)` → `rgb(10,22,34)`, blue channel
  dominant, body text at **16.07:1**, all 12 sampled tokens resolving), and the shell **works at
  phone width** (sidebar parked at `x=-248` off-canvas in a 390px viewport, hamburger slides it back
  on, `scrollWidth == clientWidth` so nothing has to be panned). I also opened
  `04-dashboard-dark.png` and `06-phone-drawer-open.png` and confirmed both by eye, since the point
  of the clause is how it looks.
- `11:47` — **one harness bug of mine worth recording**, because the failure accused the app: the
  second theme-toggle click timed out. The real `aria-label` is `Switch to ${target} mode`, so it
  names the mode you are *going to* and flips after every press — any selector containing "dark" or
  "light" matches exactly once. Fixed to `button[aria-label^="Switch to"]`.
- `11:53`–`12:05` — **[`tools/verify-checkpoint4.mjs`](../tools/verify-checkpoint4.mjs): 11 of 11**,
  the MVP loop in a browser. Product created through the form, run at REGULAR depth, second run
  against advanced fixture content, History expanded, question asked. What this closes that the
  API-level pass could not: the **live execution view** genuinely renders the backend's own strings —
  `Crawling http://127.0.0.1:8082/changelog (page 1 of ~20)`, `1 source read`,
  `Analyzing 1 source as a first baseline... (ANTHROPIC, regular depth)` — against four named steps
  with per-step status and elapsed time. Confirmed by eye in `02-run1-live-2.png` as well as by
  assertion.
- `11:58` — **a measurement problem of mine, fixed properly rather than loosened.** Polling the page
  every 100ms caught only one of the three detail strings, because against a local fixture a crawl
  step is alive for tens of milliseconds — and a poll that misses it reports the *app* as not having
  rendered it. Replaced with a `MutationObserver` installed before any app code runs, which records
  every detail line ever committed to the DOM. That is the honest instrument for "does it render as
  events arrive", and it is what turned 4.2b from a flaky pass into a real one.
- `12:03` — **the stub was hiding the streaming question, so the stub was changed.** The ask
  transcript grew in a single jump, because the stub emits 400-character chunks with no pacing: the
  whole answer lands inside a millisecond, and a UI that renders each token looks identical to one
  that waits for the last. Added `--chunk` / `--chunk-delay-ms` (defaults unchanged, so step 10's
  numbers stay reproducible); at `--chunk 24 --chunk-delay-ms 60` the transcript grows across **7
  distinct lengths**. Worth being explicit that the original observation was a property of the
  fixture, not a defect in the app — asserting it either way without checking would have been wrong.
- `12:05`–`12:09` — **the browser walk found a real defect, and it is the reason step 13 asks for
  one.** The report meta line read `Sep 21, 2026, 12:01 PM · Run by admin · changes` — a change count
  with no number. Pulling that thread found **one root cause with three visible symptoms**:
  1. `RunEvent.report` carried the **entity** `ChangeReport` while `GET .../reports` returns
     `ChangeReportResponse`. `changeCount` is *derived* in the response, so it is simply absent from
     the stream — a freshly-finished run rendered "· changes", and the same report in History
     rendered "9 changes".
  2. `ChangeCategory` and `Confidence` are serialised **lower case** by `@JsonValue wireName()`, on
     purpose, but `Badges.tsx` keyed `CATEGORY_CLASS` on `FEATURE`/`PRICING`/… and compared
     `confidence === 'HIGH'`. Every lookup missed. Measured in the browser rather than argued:
     all seven category pills resolved to `_other_15yhl_39` with background
     `rgba(18, 36, 31, 0.12)`, and all three confidence pills to `pill-neutral` — **one colour where
     the spec asks for seven**, on every screen, both paths.
  3. Same cause, third symptom: `CATEGORY_ORDER` in `ReportView.tsx` never matched either, so the
     "pricing and policy first" grouping order documented in that file was never applied. Groups
     came out in whatever order the model emitted.

  **Why nothing caught it.** The labels were right either way — `humaniseEnum('feature')` and
  `humaniseEnum('FEATURE')` both produce "Feature" — so the screens looked plausible. The 40 of 40
  acceptance pass asserts JSON fields, not rendered classes. And `ReportView` and `Badges` had **no
  frontend tests at all**: the app's central output was the largest uncovered surface in the
  frontend. A type union that was simply wrong (`'FEATURE' | …`, with a comment asserting
  "Serialised as the enum name") made TypeScript agree with the mistake.
- `12:09` — **fixed on both sides, and the fix is verified to fail without itself.** Frontend aligned
  to the wire contract (the backend's lower case is deliberate and documented; the frontend's upper
  case was not). Backend `RunEvent` now carries `ChangeReportResponse`, so a report is one shape
  however the client got it — that removes the divergence rather than patching `changeCount` alone.
  Added `Badges.test.tsx` and `ReportView.test.tsx`: **97 frontend tests in 11 files**, up from 80 in
  9. Then put the bug back deliberately and confirmed **4 of the new tests fail** with it, because a
  regression test that has never failed is not yet evidence of anything.

- `12:10`–`12:17` — **checkpoint 5, and the container is not the same app.** Built `cp5-step13` and walked
  the packaged image instead of the dev server. `/login`, `/products/{id}`, `/admin`, `/settings`, `/nope` —
  **every client route except `/` answered `401` with an `application/json` body.** Not a selector problem
  and not a login problem: there was no SPA fallback in the application at all, and `SecurityConfig`
  permitted a hand-written list of five static paths. So a bookmark, a shared link, or an ordinary browser
  reload replaced the whole app with an error payload, and the app's own `NotFoundPage` was unreachable in
  the image. Invisible in `npm run dev` because **Vite serves that fallback itself**.
- `12:25` — **second defect, found while chasing the first.** Unknown paths answered **500 INTERNAL_ERROR**,
  logged at ERROR with a stack trace: Spring's `NoResourceFoundException` fell through to the catch-all
  handler. `/assets/nonexistent.js` → 500, `/api/products` → 500. So any typo in a `curl`, or any bot
  probing for `/wp-login.php`, wrote error-level noise that reads like an outage in progress. Handled
  explicitly, with tests — `GlobalExceptionHandlerTest`, which is also the first test this class has had.
  I found this one only because I mistyped two admin paths myself and got a 500 where a 404 belonged.
- `12:28`–`12:31` — **fixed the fallback, and made the two halves share one predicate.** New
  `SpaForwardingConfig` resolves genuinely-unmapped, non-file paths to the HTML shell, and `SecurityConfig`
  now matches frontend paths by calling `SpaForwardingConfig.isFrontendPath` rather than by listing routes —
  the two cannot disagree about which paths those are, which was the actual bug rather than the missing
  handler. Before writing a matcher that broad I checked that **every controller in the app is mapped under
  `/api/**`** (SSE included), so deny-by-default is preserved; only `/actuator/**`, `/v3/api-docs/**` and
  `/swagger-ui*` sit outside it, and those are named explicitly. The resolver logic was extracted to a
  `static Resolution resolutionFor(String, boolean)` so it is testable without a servlet container:
  `SpaForwardingConfigTest`, 23 cases over client routes, server-owned paths and file-like paths.
- `12:35`–`12:36` — **third defect, and the one I would have missed by watching the UI.** Every SSE stream
  was **truncated rather than terminated**. Every event arrived and the screen looked correct, so nothing in
  the browser said "broken" beyond one console line per run — but `curl` exited **18**
  (`transfer closed with outstanding read data remaining`) and browsers logged
  `ERR_INCOMPLETE_CHUNKED_ENCODING`. Measured at the protocol level rather than inferred: the payload was
  **complete** — 3225 bytes, 10 event blocks, the final `run_completed` parsing cleanly with
  `changeCount: 3`. Only the chunked terminator was missing. The container log had the cause:
  `AuthorizationDeniedException: Access Denied` on `CoyoteAdapter.asyncDispatch`, then "response is already
  committed", then a failed `/error` render.
- `12:36` — **proved it pre-existing instead of assuming it.** Ran the *pre-change* image `cp5-step13` and got
  the same `curl` exit 18 with four `AuthorizationDeniedException`s — so this was not something my
  `SecurityConfig` change had introduced, which was the first thing I suspected and the cheapest thing to
  rule out.
- `12:50` — **root cause: a default I had never had reason to look at.** `JwtAuthenticationFilter extends
  OncePerRequestFilter`, whose **`shouldNotFilterAsyncDispatch()` returns `true` by default** — so the filter
  is skipped on the dispatch that completes an `SseEmitter`, while Spring Security's `AuthorizationFilter`
  *does* run there (Boot's default `spring.security.filter.dispatcher-types` includes ASYNC). A session-based
  app would never notice, because the context would be restored from the session; a stateless one has nothing
  to restore it from, so **the run's own stream is denied as anonymous at the exact moment it finishes**. One
  boolean override. Dev hid this too: Vite's proxy terminates the stream to the browser itself.
  `JwtAuthenticationFilterTest` added — 6 tests, and the filter had **none** before. Put the bug back and
  confirmed **2 of the 6 fail** without the fix.
- `12:50` — **backend suite: 801 tests**, 0 failures / 0 errors / 0 skipped, JaCoCo over 173 classes, up from
  769 over 170. All 32 new tests are on surfaces that had no coverage at all: the auth filter, the exception
  handler's fall-through, and the SPA resolver.
- `12:51`–`12:57` — **re-measured everything against the final image.** SSE: `curl` **exit 0**, last event
  `run_completed`, and `grep -c AuthorizationDeniedException` in the container log now **0** (was 4).
  Checkpoint 4 in the browser: **11/11**, including `4.7 no uncaught browser errors`, which was the one check
  still failing on the previous image — its only failure had been the `ERR_INCOMPLETE_CHUNKED_ENCODING` line.
  Frontend: **97 tests in 11 files**.
- `12:59`–`13:02` — **checkpoints 1 and 2 re-walked against the fixed tree**, not left standing on
  yesterday's evidence, since the backend had changed since. CP1: JDK 26.0.2.1, Maven 3.9.16, Node v22.23.2,
  Docker 29.6.2 client / 29.5.3 server. CP2 on the host against an **empty** database: started in 2.015s,
  both first-boot banners, health exactly as documented, all six checks matching including `totalElements: 3`
  and `LLM_CREDENTIAL_ADDED {"provider": "ANTHROPIC"}`, and **zero ERROR-level lines**. Used a throwaway
  MongoDB on `27018` rather than resetting the existing dev volume, so the empty-database path was exercised
  for real without destroying anything of the human's.
- `13:02` — **checked my own fix on the path it was not designed for.** `SecurityConfig` now permits any
  non-`/api` GET, and on the host there is no frontend on the classpath at all — so I confirmed that `/`,
  `/login` and `/nope` answer a clean **404** there rather than 401, 500, or something worse, while every
  `/api/**` path is still 401 unauthenticated and 200 with a token. A broad `permitAll` matcher deserves
  that check in both shapes.
- `13:03`–`13:10` — **made the harnesses survive a fresh clone.** `verify-checkpoint3/4.mjs` were resolving
  Playwright through symlinks in a repo-root `node_modules/` pointing into `/tmp` — fine for one afternoon,
  dead on arrival for anyone else, and the docs now cite these scripts as the evidence for two checkpoints.
  Gave `tools/` its own `package.json` (`node_modules/` was already gitignored), deleted the symlink farm,
  and re-ran both from the committed manifest: **11/11 and 11/11**. A documented harness that only runs on
  the machine that wrote it is not documentation.

- `13:10`–`13:22` — **the documentation half of step 13**, which is what the step actually asked for. Rewrote the
  `GETTING_STARTED.md` status table so all five rows describe **one build on one day** and name the vantage point of
  each walk, and added a section recording the three defects the checkpoint-5 walk found — the doc tells you to chase
  a container/dev divergence, so what happened when someone did belongs in it. Replaced the README's 42-line accreted
  build-status block with 16 lines that don't duplicate `EVAL_LOG.md`, and deleted its "each needs a human at a
  browser to finish" framing, which today falsified. Documented the two browser harnesses in `SETUP.md`. Wrote
  **ADR 0009** for the SPA fallback and the shared frontend predicate, and added the async-dispatch consequence to
  **ADR 0002**, where it belongs as fallout of the stateless-auth decision. Propagated the `/api` security boundary to
  `AGENTS.md`, `ARCHITECTURE.md` and the `add-a-new-endpoint` skill; added the truncated-stream diagnosis to
  `troubleshoot-running-instance`, the walk-the-image rule to `local-dev-loop`, and the unmatched-path 404 to
  `API.md` — verified verbatim against the running container rather than transcribed from the test.
- `13:20` — **checked my own documentation the way the `update-docs` skill says to.** Wrote a link checker over all 33
  markdown files: every internal link and heading anchor resolves. It first reported
  `SETUP.md#-the-default-admin-credential` as broken, which was **my checker being wrong, not the doc** — GitHub
  strips a leading emoji but keeps the space after it, so the leading hyphen is correct. Worth recording because the
  instinct was to "fix" the doc.

## End of session — 13:22

**One task, and it turned into two.** Step 13 is a documentation step. Re-walking checkpoint 5 honestly
turned up **three real backend defects**, all fixed with tests:

| Defect | Why nothing caught it |
|---|---|
| Every client route but `/` answered 401 JSON | Vite serves the SPA fallback in development, so the application never needed one until it was packaged |
| Unknown paths answered 500, logged at ERROR with a stack trace | Nothing in the app requests a path that doesn't exist — until a human mistypes one, no code path reaches it |
| Every SSE stream truncated, never terminated | The UI looked correct, because every event *did* arrive; the defect was in the framing of a complete response, and Vite's proxy terminates the stream itself |

Two of the three were masked by a *convenience* of the dev server. That is the argument for the checkpoint
doc's own instruction — walk the packaged artifact, and treat any divergence from the dev loop as a bug —
and it is the reason step 13 asks for the re-walk rather than trusting the earlier ones.

**Both suites green**: backend **801** (0 failures, 0 errors, 0 skipped, JaCoCo 173 classes), frontend **97**
in 11 files. **All five checkpoints walked against one finished build**, 3 and 4 in a real browser at 11/11
each — the first time this repo's checkpoint table has described a single build rather than five different
ones.

**Definition of Done movement.** Of the five open clauses, the appearance clause is **closed by measurement
and by eye** (navy not black, 16.07:1, seven distinct category pills where there had been one colour, the
live view rendering the backend's own strings as they arrive), and phone width is **half-closed** — the shell
and Dashboard are verified at 390px with no overflow; the product-detail and admin screens are not. Still
open, and honestly so: the **OpenAI provider has never run end to end**, **analysis quality is unjudged**
because a stub has no analysis to judge, and **nobody has followed `GETTING_STARTED.md` from a fresh clone on
a machine that never built this**.

**Carried over**: nothing is committed — all of the above is in the working tree. Also uncommitted and worth
a decision: `tools/verify-checkpoint3.mjs`, `tools/verify-checkpoint4.mjs` and `tools/package.json`, which the
docs now link to and therefore need to ship.

**One thing I would do differently.** I reached for a browser only at step 13, on the last day, because every
earlier session had recorded "needs a human at a browser" as a known gap and moved on. Playwright had been
available by `npx` the whole time. Four of the five DoD clauses that stayed open for the entire build, and all
three defects found today, were reachable from the machine I was already on — the gap was not capability, it
was never having tried.
