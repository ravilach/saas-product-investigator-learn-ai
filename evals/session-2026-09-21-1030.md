# Session: 2026-09-21 10:30 (start)

> **Live-logged.** Every entry below was written when the thing happened, from the system clock
> (`date`). This is the first session file in this repo that is live-logged from its first line —
> the two 2026-09-20 files were both reconstructed after the fact.
>
> First session of a new day, and the first one that opens with the build already 11 of 13 steps in.

- **Starting commit**: `c844da5361aa409013ee3e31e11445eec26c25ed` — "Hardened Mongo Signature"
  (2026-09-20 `20:38`). Working tree clean at session start.
- **Spec version**: `a912316` (2026-09-20 `17:52`), content hash
  `80416c78de8e251f77cc9353727138fd21676427` — unchanged since it was first tracked.

## Log

- `10:30` — **human**: _"Where did we stop in our progress? Also we can make sure to create a new
  EVALs today."_ Two things: establish current state, then log going forward.
- `10:30`–`10:35` — **state review.** Deliberately done against the repo and the machine rather than
  against the docs, since the previous eval had already flagged the docs as the thing most likely to
  be stale. What that turned up:
  - `Dockerfile` (15,189 bytes) and `entrypoint.sh` (executable) **exist and are committed** — the
    gap the `19:28` eval called "the single biggest" is closed.
  - `docker image inspect saas-investigator:latest` → created `2026-09-20T20:17:53-04:00`, i.e.
    **before** `c844da5` (`20:38`) rewrote the MongoDB-key block. The image on disk does not
    correspond to HEAD.
  - Nothing left running: no Vite dev server, no `spring-boot:run`, no containers
    (`docker ps` empty). The three stragglers the previous eval asked to clean up are gone.
  - No step-10 artifact of any kind (`verify-step9.mjs` / `verify-step10.mjs` both absent), and no
    transcript covering an acceptance pass. **The step-10 session that was mid-flight at `19:28`
    produced nothing.** Recorded plainly rather than left as "unknown".
  - `EVAL_LOG.md` still does not exist.
- `10:31`–`10:34` — **found an unlogged hour**: four sessions and four commits between `19:39` and
  `20:38` last night (all of step 11) had no eval file, because the previous file closed at `19:28`.
  Reconstructed it into `evals/session-2026-09-20-1939.md` from transcripts + `git log` + the image
  creation timestamp, labeled reconstructed. The honest read on that hour: **both defects that
  mattered were caught by the human, not by Claude's verification** — a build that only passed
  behind a `--secret` CA mount the documented command never uses, and a vendored signing key that
  was `.gitignore`'d, which would have broken the build for everyone who cloned the repo.
- `10:35` — **two claims of mine in that reconstruction were wrong on first write and are corrected
  in the file**: the `git diff --stat` line (I wrote 6 files / 526 insertions; actually 9 files /
  525 / 4 deletions), and "README not touched after `20:01`" — it *was* edited at `20:01`/`20:05`,
  which makes the staleness worse, not better: the quick start's new port explanation was added four
  lines above a line still saying the image doesn't exist yet.
- `10:36` — this file created. Sequencing note: it is timestamped `10:30`, the session start, not
  `10:36`, per the tracking prompt.
- `10:37` — **human**: _"Let's finish the remaining tasks"_ — the four carried-over items, as a set.
- `10:37` — **human denies a permission prompt.** Claude asked to run one command that would have
  inspected `deploy/.env`, grepped the environment for `ANTHROPIC_*`/`OPENAI_*`, and listed
  `~/.anthropic*`. Denied. Recorded because it changed the approach rather than just costing a turn:
  the question "is there a usable provider key on this machine" was never answered, and the whole
  mock-provider path below follows from not answering it. The redaction in the command was not the
  point — the human's boundary is that the file is not read at all.
- `10:39` — **human**, asked how to obtain a provider key for step 10: _"Try to mock it "_. This is
  the decision the rest of the session turns on. It makes step 10 answerable today, and it
  permanently bounds what a green step 10 can mean: the pipeline, not the analysis.
- `10:39` — **carried item 2 closed**: `docker build --no-cache` against HEAD, producing
  `saas-investigator:head` at `10:39:48`. The hardened MongoDB-key block builds as part of the real
  image, not just as a `/tmp` fragment. **On this network it needs
  `--build-arg MONGODB_GPG_INSECURE=1`** — the corporate TLS interception from last night is
  unchanged, and the documented flag is what gets past it. That is the documented failure behaving
  as documented, which is the useful result.
- `10:40`–`10:45` — **the seam that makes mocking possible, and the argument for it.** Neither
  provider class could be pointed anywhere but at the provider. Added
  `ANTHROPIC_BASE_URL`/`OPENAI_BASE_URL` (`app.llm.anthropic.base-url`, `app.llm.openai.base-url`)
  — validated at startup, logged when set, absent by default. Worth being explicit that this is a
  change to production code made in order to test: the alternative was mocking at a Java seam, which
  would have tested a test double instead of the SDK calls the app actually makes. The env var is
  also independently useful (a gateway, a proxy, a self-hosted compatible service), which is the
  test for whether a testing seam is one.
- `10:45` — **the two fixtures**, in [`tools/mock-llm/`](../tools/mock-llm/): an Anthropic-shaped
  stub that streams well-formed reports and honours the structured-output schema, and a fake product
  site with two revisions of `/changelog` and `/pricing` plus a `robots.txt` that disallows a marked
  page. The stub reads the depth out of the **prompt's own instruction text** and sizes its reply
  accordingly, which is what makes "do Short/Regular/Nuclear actually differ" a real assertion rather
  than a restatement of what the harness asked for.
- `10:45`–`11:01` — **[`tools/verify-step10.mjs`](../tools/verify-step10.mjs)**, 748 lines: every
  clause of step 10 as one numbered assertion, exiting non-zero on any failure. Before running it,
  the field and action names it assumed were checked against the real controllers rather than
  trusted — which caught three invented audit action names (`LOGIN_SUCCESS`, `RUN_STARTED`,
  `SETTINGS_UPDATED`; the real ones are `AUTH_LOGIN_SUCCESS`, `PRODUCT_RUN_TRIGGERED`,
  `SYSTEM_SETTINGS_UPDATED`) and an assumption that the SSE `run_completed` event carries a
  `changeCount`, which it does not — it carries the whole `ChangeReport`. All four would have been
  reported as application failures.
- `11:01` — **first run: 6 of 13, then an uncaught `TypeError`.** Every run failed inside the
  provider call, and every crawl failed with "robots.txt could not be fetched (connection refused)"
  against fixtures that were plainly running. **The cause is an environment fact worth writing down:
  on this machine `host.docker.internal` resolves to `172.17.0.1`, the Linux VM's bridge gateway —
  not the macOS host.** Confirmed from inside the container with `getent hosts` and two `curl`s
  exiting 7. A listener started on the host is simply unreachable that way, whatever
  `--add-host=host.docker.internal:host-gateway` implies. Fixed by running both fixtures as
  containers on a user-defined network (`siv10net`) and addressing them by container name, which
  means the same thing on every Docker host. The harness's own default flags now encode that
  topology so the next person does not rediscover it.
- `11:03`–`11:09` — **three harness bugs of Claude's own, each of which first looked like an
  application bug.** Recorded individually because the pattern is the point — a failing assertion is
  evidence about the assertion too:
  1. **"PDF has 0 pages."** The writer uses cross-reference and object streams, so the page tree is
     Flate-compressed and searching raw bytes for `/Type /Page` finds nothing — indistinguishable
     from a broken PDF. Proved the document correct by inflating the streams and reading real text
     out of it; the check now asserts content.
  2. **"DOCX is only 4,256 bytes."** A size threshold, not an assertion. The package contains
     49,371 bytes of `word/document.xml`, 113 paragraphs and 25 table rows for 24 changes. The check
     now unzips it and counts rows.
  3. **"`runsLast24h` is 5, expected 7."** 5 was correct; the 7 was hand-written. Replaced with a
     counter the harness increments on each accepted run — a hardcoded expectation is a second thing
     that can be wrong, and when it is, it accuses the application.
- `11:05` — **one real application bug, found by the harness and fixed with tests.** A custom-range
  Compare ending **today** was rejected with _"A comparison range cannot extend into the future"_.
  `CompareRequest.toInstant()` resolves `toDate` to the *end* of its day, precisely so that picking
  today includes the run you just did — and `HistoryLoader.validateRange` then compared that instant
  against `Instant.now()`. So for all but the last millisecond of any day, **the most natural range a
  user can pick was refused.** The same mismatch made the earliest-data message tell a user whose
  product was first run today to _"start the range on or after 21 Sep 2026"_ — the date they had
  just picked, an error whose own advice is what the user already did. Both comparisons are now
  day-granular in UTC, which is the unit the request is actually expressed in; 2 new tests, 27
  passing in `HistoryLoaderTest`. **This is the bug that justifies the whole harness**: it is not
  reachable by unit tests that construct their own instants, and a human clicking "today" would have
  hit it on their first Compare.
- `11:09` — **run 3 still failed one check, and the harness was right to.** It asked for
  yesterday→today on a product whose data starts today, which is *correctly* a 400. Fixed the range
  and added a check asserting the negative case still holds, so the fix above cannot quietly become
  "accept anything".
- `11:10` — **step 10 is green: 40 of 40.** 37 in the main pass (`HARNESS EXIT: 0`), plus 1 for
  restarting the container and confirming the persisted JWT secret is reused rather than signing
  everyone out, plus 2 in a second container started with **no** provider key, which cannot be
  checked in the same process while a key is in the environment.
- `11:11` — **checked the mock's request log rather than trusting the green.** 27 requests: the
  depth instruction was derived from the prompt's own text on every report call (never from the
  `max_tokens` fallback), prompts ran 3,877–17,033 characters, `max_tokens` was 4,096/16,384/64,000
  per depth, structured output was requested on every report, and every source attribution parsed.
  The green means what it says.
- `11:11`–`11:12` — **both suites re-run after the `HistoryLoader` change.** Frontend: **80 tests in
  9 files**, `NPM EXIT: 0`. Backend: **769 tests, 0 failures, 0 errors, 0 skipped**, JaCoCo over 170
  classes, `BUILD SUCCESS`, `MVN EXIT: 0`, finished `11:12:06`.
- `11:13`–`11:20` — **carried items 3 and 4 closed.** `README.md`'s build-status block and
  `docs/GETTING_STARTED.md`'s checkpoint table now describe the state actually verified today;
  `docs/SETUP.md` documents the two new base-url vars and the mock harness. Removed
  `docker/mongodb-server-8.0.asc`, its `.gitignore` entry, and the now-empty `docker/` directory.
- `11:14` — **a self-correction worth logging as an intervention even though no human made it.**
  Claude wrote _"checked by eye instead"_ about dark mode into the README **without having opened a
  browser**. Caught on re-reading, before the human saw it. The passage now states precisely what is
  verified (23 colour tokens each have a dark counterpart, no dark-only token, 5 toggle tests) and
  what is not (that it *looks* right, and that contrast holds on every screen). The near-miss is the
  data: the pressure to write a status block as finished is strongest exactly where the evidence is
  thinnest.
- `11:19` — **found a misnumbering in Claude's own README**: _"What's left: step 12 (the Definition
  of Done sweep) and step 13"_. Step 12 is the k8s/ECS samples and the two Harness pipelines, all of
  which exist and have since `19:33` last night; the Definition of Done is its own section of the
  spec, not a numbered step. With step 10 closed the real count is **12 of 13**, and what remains is
  step 13 plus the DoD sweep. Found by reading the spec's BUILD ORDER rather than the README.

- `11:20`–`11:27` — **`EVAL_LOG.md` written** (interim, see below), linked from the README, and the
  test containers, network and stale image tag cleaned up. One claim was caught and corrected while
  writing it: the rollup initially listed "`docker build` succeeds with a deliberately broken test"
  among the clauses with first-hand evidence. Nobody has ever broken a test and rebuilt — the clause
  holds **by construction**, since `mvn package -DskipTests` never invokes the suite. Met by
  construction and met by experiment are different claims and the file now says which this is.

## End of session — 2026-09-21 11:27

- **Ending commit**: _uncommitted._ Everything below is in the working tree; the session's work has
  not been committed, which is itself the first carried-over item.
- **Files changed**, `git diff --stat c844da5` plus untracked:
  ```
   13 files changed, 229 insertions(+), 32 deletions(-)
   + tools/ (3 files, 1,094 lines), LlmPropertiesTest.java (103), 2 eval files
  ```
  Application code touched: `HistoryLoader` (the compare fix), `LlmProperties` +
  `AnthropicLlmProvider` + `OpenAiLlmProvider` (the base-url seam), `application.properties`. Docs:
  `README.md`, `docs/GETTING_STARTED.md`, `docs/SETUP.md`. Tests: `HistoryLoaderTest` (+2),
  `LlmPropertiesTest` (new), 3 test files touched by the constructor change. Removed:
  `docker/mongodb-server-8.0.asc` and its `.gitignore` entry.
- **Tests**: **passing, measured this session at `11:11`–`11:12`**, after the `HistoryLoader` change.
  - Backend: `mvn test` → **769 tests, 0 failures, 0 errors, 0 skipped**; JaCoCo over 170 classes;
    `BUILD SUCCESS`. Up 11 from last night's 758 (+2 `HistoryLoaderTest`, +9 `LlmPropertiesTest`).
  - Frontend: `npm test -- --run` → **80 tests in 9 files**, unchanged; no frontend code was touched.
  - Every build and test command in this session echoed its exit code **into** the log file, because
    `mvn -q` and any pipeline ending in `tail`/`grep` hide a non-zero status. That habit is what made
    the three harness bugs above debuggable rather than confusing.
- **Steps completed this session**: **step 10** — the end-to-end acceptance pass, 40 of 40 checks,
  against the real container image. That takes the build from **11 of 13 to 12 of 13**: steps 1–12
  are done and only step 13 remains. Also corrected the count in the README, which had step 12 listed
  as outstanding under the wrong description.
- **What step 10 green does and does not mean.** It means crawl, robots, prompt construction, the
  provider call, structured-output parsing, persistence, SSE narration, PDF and DOCX rendering,
  Compare, RBAC, secret masking and auditing all work end to end against the packaged artifact. It
  means nothing about analysis quality — with a stub provider there is no analysis. Two clauses are
  outside what an API-level harness can reach and are covered structurally rather than visually: dark
  mode's *appearance*, and watching the live execution view render as events arrive. Both need a
  human at a browser, and the README says so in those words.
- **Interventions — 2 substantive human messages and 1 permission denial**, in 50 minutes. All three
  mattered:
  1. `10:37` the **denied credential inspection**, which foreclosed the live-key path.
  2. `10:39` _"Try to mock it "_ — four words that decided the entire approach, and made step 10
     answerable on a day with no provider key.
  3. `10:37` _"Let's finish the remaining tasks"_ — routine scoping, but it is what set the four
     items rather than one.

  Also logged: **one self-caught false claim** (`11:14`, "checked by eye" about dark mode) and **one
  self-caught misnumbering** (`11:19`, step 12 in the README). Neither was found by a human, which is
  the difference from last night's session, where both defects that mattered were.
- **Context compactions: 2** (`10:52`, `11:16`). Both fell mid-task and neither lost work, because
  the harness and its results were on disk rather than in context — `tools/verify-step10.mjs` and
  `/tmp/step10-final.log` are readable after a compaction in a way a conversation is not. That is a
  general argument for writing acceptance passes as scripts.

## Carried over

- **Nothing is committed.** 13 modified files and 4 new paths (`tools/`, `LlmPropertiesTest`, two
  eval files) are in the working tree only. The single most valuable next action.
- **Step 13** — finalise the docs and re-walk all five `GETTING_STARTED.md` checkpoints against the
  finished app — plus the **Definition of Done sweep**, which is its own section of the spec. Both
  need a browser for the parts step 10 could not reach.
- **The OpenAI provider is not verified end to end, and step 10's green does not cover it.** Found
  while writing the rollup below, by reading the Definition of Done against the harness rather than
  against the step-10 clause list: the DoD requires adding an OpenAI key, switching
  `preferredLlmProvider`, and confirming a run actually used OpenAI. The stub is Anthropic-shaped and
  [`tools/verify-step10.mjs`](../tools/verify-step10.mjs) does not contain the string "openai". The
  class has unit tests; the provider-switch path has never run. **Cheap to close now** — the
  `OPENAI_BASE_URL` seam added this session is the other half of the same knob, so it needs an
  OpenAI-shaped stub and about six more checks. Recorded as a gap rather than rolled into "40 of 40",
  which is a count of step-10 clauses and was never a count of DoD clauses.
- **`EVAL_LOG.md` is written this session**, as an interim rollup rather than a final one, since step
  13 is not closed. It needs one more pass when it is. The tracking prompt's reference to "that
  file's existing template" still has no referent; the structure used was derived from the prompt's
  own list of required contents.
- **Nothing left running — cleaned up at `11:26`**, unlike the previous two sessions, both of which
  carried this item forward. Removed: containers `siv10`, `siv10np`, `mock-llm`, `fake-site`, the
  `siv10net` network, and a stray `beautiful_euclid` from last night. **`saas-investigator:latest` now
  points at the verified image** rather than at the `20:17` build that predated HEAD — that mismatch
  was the one a naive `docker run saas-investigator` would have picked up, and it was flagged as a
  carry-over this morning. `spi-mongo` is left in place, stopped: it holds the local dev database.
- **Not a gap, recorded so it is not rediscovered**: `host.docker.internal` does not reach the macOS
  host on this machine. Any future harness should use a user-defined network and container names.
