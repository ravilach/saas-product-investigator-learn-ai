# Session: 2026-09-20 17:37 (start)

Live-logged. Session start time `17:37:45` is exact — taken from the first entry of this
session's transcript (`0177423a`), not estimated. Timestamps below come from `date` at the
moment each entry was written.

- **Starting commit**: `9ce98453ed6f88b3e3759d7e9398eb1ad7e578fc`
- **Spec version**: untracked, no commit hash; content hash
  `80416c78de8e251f77cc9353727138fd21676427` (unchanged from the reconstructed session)

> **Concurrency note, relevant to reading this file:** a second Claude session (`df4aa07a`) was
> building the backend at the same time as this one and kept writing files throughout
> (`AdminSettingsControllerTest.java` at 17:44, mid-session). **None of the backend work
> timestamped during this session is mine** — this session only touched `.claude/settings.json`
> and `evals/`. The 247-file working tree is that other session's output.

## Log

- `17:37` — session starts. Task: add common-sense shared `.claude/settings.json` for anyone
  cloning the repo. Not a BUILD ORDER step — tooling/repo hygiene.
- `17:39` — wrote `.claude/settings.json`: `permissions.allow` (safe mvn/npm/git/docker read-only
  + build commands), `deny` (secret files, `rm -rf /*`, force-push), `ask` (push/publish/deploy),
  `additionalDirectories: ["/tmp"]`. Verified valid JSON and confirmed not gitignored.
- `17:40` — self-caught before handing off: `Read(./.env.*)` in the deny list would also have
  blocked `.env.example`, the one env file that's meant to be read. Narrowed to
  `.env` / `.env.local` / `.env.*.local`. Not a human correction — no human input involved.
- `17:45` — new task: set up this `/evals` tracking habit. Began by gathering real data rather
  than writing the files first.
- `17:45` — **found: `git log` is unusable as the reconstruction source the instructions
  assumed.** Only 2 commits exist and all build work is uncommitted, so git history records
  almost none of it. Switched the retroactive reconstruction to file mtimes + session transcripts,
  and said so in that file's header instead of quietly substituting sources.
- `17:45` — **found: no commit hash exists for "Spec version"** — `docs/` is untracked. Recorded
  `git hash-object` content hash instead, flagged as a real gap.
- `17:45` — **found: `EVAL_LOG.md` does not exist**, though the instructions refer to "that
  file's existing template". Nothing created for it — the rollup is an end-of-build task and the
  build is mid-step-7. Raised to the human instead of inventing a template.
- `17:46` — **found: a second Claude session is building concurrently** (18 MB transcript, writes
  at 17:42/17:44) and a `spring-boot:run` has been up ~2h54m. Recorded so this session's file
  doesn't claim credit for that work.
- `17:46` — recovered human interventions from session transcripts (real per-message timestamps,
  verbatim quotes) rather than leaving them unknown: 3 human touchpoints in sessions 1–2, and
  **zero during the 5h21m unattended build**, plus 16 auto-compactions.
- `17:46` — created `evals/` + `evals/session-2026-09-20-1042.md` (reconstructed, labelled as
  such in its header).
- `17:47` — created this file.
- `17:50` — corrected this file: I had stamped the end block `17:48`, which was a guess made while
  writing rather than a reading from `date`. Actual time was `17:50` and the session was still
  open. Fixed rather than left standing — inventing a close time is exactly the failure this log
  is supposed to catch.

## End of session — 2026-09-20 17:50 (written while session still open)

> Written at `17:50`, before the session actually ended, so the human gets a closed file rather
> than a dangling one. Anything after `17:50` in this session is appended to the Log above and
> this block updated; it is not a sealed record yet.

- **Ending commit**: `9ce98453ed6f88b3e3759d7e9398eb1ad7e578fc` (unchanged — nothing committed
  this session, matching the rest of the repo's state)
- **Files changed**: no tracked files touched by this session, so `git diff --stat` shows nothing
  of mine. Added as untracked: `.claude/settings.json`, `evals/session-2026-09-20-1042.md`,
  `evals/session-2026-09-20-1737.md` (3 files). The `git diff --stat 9ce9845 <HEAD>` the template
  asks for is empty because start and end commit are identical.
- **Tests**: **not run this session.** No code changed. (The concurrent session was running tests
  during this window; that is not this session's result to report.)
- **Steps completed this session**: none from BUILD ORDER — this session was tooling only
  (`.claude/settings.json`) and eval-tracking setup.
- **Carried over to next session**:
  - `EVAL_LOG.md` does not exist and has no template — needs one before any end-of-build rollup.
  - The concurrent build session's step 7 is unfinished and **uncommitted**; its session file has
    an intentionally open end block that a future session should close.
  - Worth deciding: whether the build-prompt spec should be committed, so "spec version" can be a
    real hash going forward.
